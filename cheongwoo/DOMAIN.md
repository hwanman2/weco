# 도메인 붙이기

준비는 끝났습니다. **도메인만 사시면 됩니다.**

---

## 1. 어느 이름을 살까

2026-08-18 기준 DNS 조회 결과입니다. (등록 여부는 등록업체 화면에서 한 번 더 확인하세요)

| 도메인 | 상태 | 의견 |
|---|---|---|
| **`cheongwoohaejang.com`** | 비어 있음 | **추천.** 매장 이름 전체라 제품 브랜드와 안 겹칩니다 |
| `cheongwoo-haejang.com` | 비어 있음 | 하이픈은 광고에서 읽기 나쁩니다. 차선 |
| `cheongwoohaejang.kr` | 비어 있음 | 국내 전용이면 무난. 외국인 손님엔 `.com` 이 낫습니다 |
| `cheongwoohaejang.co.kr` | 비어 있음 | 길어서 비추천 |
| ~~`cheongwoo.kr`~~ | **이미 등록됨** | 쓸 수 없고, 써서도 안 됩니다 |

### 왜 `cheongwoo.kr` 같은 짧은 이름을 피하나
「청우」만 쓰면 **청우식품(이음식 브랜드)** 과 계속 겹칩니다. 그쪽 검색량이
매장의 2.5배라, 짧은 이름을 쓰면 남의 브랜드 검색에 묻힙니다.
`cheongwoohaejang` 처럼 **매장 이름 전체**를 쓰는 게 구분에 유리합니다.

### 한글 도메인(`청우해장.kr`)은 비추천
광고의 표시 URL에서 퓨니코드(`xn--2q1bx9k...`)로 깨져 나올 수 있습니다.

---

## 2. 어디서 사나

| 등록업체 | 연 비용(.com) | 비고 |
|---|---|---|
| 가비아 gabia.com | 2만원 안팎 | 국내 최대. 한국어 지원, 카드 결제 편함 |
| 후이즈 whois.co.kr | 2만원 안팎 | 국내 |
| Cloudflare | 1.5만원 안팎 | 원가 판매라 가장 쌈. DNS 관리가 제일 편함. 영문 |
| Namecheap | 1.5만원 안팎 | 영문 |

**처음이시면 가비아**가 편합니다. 한국어로 되어 있고 결제·갱신이 익숙합니다.
**DNS를 자주 만지실 거면 Cloudflare** 가 낫습니다.

### 살 때 체크할 것
- **자동 갱신 켜 두세요.** 도메인 만료로 사이트가 죽는 게 가장 흔한 사고입니다.
- **후이즈 정보 보호(개인정보 보호)** 옵션을 켜세요. 대개 무료입니다.
  안 켜면 등록자 이름·주소·전화번호가 공개되고 스팸 전화가 옵니다.
- 부가서비스(호스팅·메일·SSL)는 **전부 필요 없습니다.** 도메인만 사세요.
  GitHub Pages 가 무료로 호스팅하고 SSL 인증서도 자동으로 발급합니다.

---

## 3. 산 다음 DNS 설정

등록업체의 「DNS 관리」 화면에서 아래를 넣습니다.
`cheongwoohaejang.com` 을 샀다고 가정한 예시입니다.

### A 레코드 4개 (루트 도메인용)
| 타입 | 호스트 | 값 |
|---|---|---|
| A | `@` | `185.199.108.153` |
| A | `@` | `185.199.109.153` |
| A | `@` | `185.199.110.153` |
| A | `@` | `185.199.111.153` |

### CNAME 1개 (www 용)
| 타입 | 호스트 | 값 |
|---|---|---|
| CNAME | `www` | `hwanman2.github.io.` |

> 위 IP 는 GitHub Pages 의 공식 주소입니다. 바뀌는 일이 드물지만,
> 설정 전에 GitHub 문서(Managing a custom domain for your GitHub Pages site)에서
> 한 번 확인하시면 확실합니다.

DNS 반영에는 보통 10분~1시간, 길면 하루가 걸립니다.

---

## 4. 제가 할 일 (도메인 정해지면 알려 주세요)

1. `src/store.mjs` 의 `customDomain` 에 도메인 입력 → `node build.mjs`
   - 5개 언어의 canonical · hreflang · og:url
   - sitemap.xml
   - 구조화 데이터의 모든 주소
   - GitHub Pages 용 `CNAME` 파일
   이 **전부 한 번에** 새 도메인으로 바뀝니다. (양쪽 모드 다 테스트해 뒀습니다)

2. **별도 저장소로 분리** — GitHub Pages 는 저장소 하나당 도메인 하나입니다.
   지금 `weco` 저장소에 도메인을 붙이면 위코컴퍼니 사이트까지 통째로
   옮겨가 버립니다. 그래서 청우해장만 새 저장소로 떼어냅니다.
   이미 준비해 두었습니다 — 이미지를 `cheongwoo/images/` 안으로 넣어서
   **이 폴더만 통째로 복사하면 그대로 돌아갑니다.** 상위 폴더 의존이 없습니다.

3. 저장소 설정 → Pages → Custom domain 입력 → **Enforce HTTPS 체크**

---

## 5. 도메인이 바뀐 뒤 꼭 다시 해야 하는 것

이걸 빠뜨리면 광고와 검색이 끊깁니다.

- [ ] **구글 서치콘솔** — 새 도메인으로 속성 다시 등록 + 사이트맵 재제출
- [ ] **네이버 서치어드바이저** — 동일
- [ ] **메타 도메인 인증** — 사업 관리자 → 브랜드 가치 보호 → 도메인.
      이게 되어야 iOS 전환 이벤트(AEM 8개)를 청우해장 단독으로 쓸 수 있습니다.
      **지금 `hwanman2.github.io` 로는 위코컴퍼니와 8칸을 나눠 써야 합니다.**
      도메인을 사는 가장 큰 실익이 이것입니다.
- [ ] **구글 광고** — 최종 URL 전체 교체
- [ ] **메타 광고** — 링크 전체 교체
- [ ] **네이버 스마트플레이스 · 구글 비즈니스 프로필** — 홈페이지 주소 교체
- [ ] **네이버 블로그·인스타 프로필** 링크 교체

### 순서가 중요합니다
**광고를 돌리기 전에 도메인을 붙이세요.** 광고를 먼저 돌리고 나중에 옮기면
전환 학습이 초기화되고, 쌓인 색인과 리마케팅 모수를 상당 부분 잃습니다.
지금은 아직 아무것도 안 쌓여 있어서 **잃을 게 없는 가장 좋은 시점**입니다.
